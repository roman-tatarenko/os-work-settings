# Operating System Work Settings

## SHELL Configuration

For complete SHELL setup, simply run the script:  
`shell-configuration.sh`

To run the script, use the following command in your terminal:

```bash
bash shell-configuration.sh
```
